package com.example.administrator.employeenote.utils;

import com.example.administrator.employeenote.entity.AgendaData;
import com.example.administrator.employeenote.entity.EventData;

import java.util.ArrayList;
import java.util.List;

/**
 * Created by GE11522 on 2016/11/16.
 */

public class AgendaUtils {
    public void addEvent(List<AgendaData> adatas, List<EventData> edatas) {
//        List<EventData> ecaches = new ArrayList<>();
//        for (int i = 0; i < edatas.size() - 1; i++) {
//            if (edatas.get(i).getDtstart())
//
//        }
//        String year = DateUtils.unixToString(edata.getDtstart()).substring(0, 3);
//        String month = DateUtils.unixToString(edata.getDtstart()).substring(5, 6);
//        String date = DateUtils.unixToString(edata.getDtstart()).substring(8, 9);
//        adatas.add(new AgendaData(year, month, date, new ArrayList<EventData>().add(edata)));
    }
}
